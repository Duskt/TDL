export type X = {} is an anonymous interface which is the structure of type X
python equivalent typeddict
called a structure

export interface X {} is an interface X
python equivalent class
called an object